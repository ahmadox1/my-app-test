name: ai_recipe_chef
description: تطبيق Flutter لاقتراح وصفات ذكية باستخدام نموذج ذكاء اصطناعي مفتوح المصدر مع واجهة عربية.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: '>=3.22.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  http: ^1.2.0
  google_fonts: ^6.2.1
  intl: ^0.20.2

flutter:
  uses-material-design: true

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2
